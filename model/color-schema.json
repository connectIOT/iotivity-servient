{
  "title": "st.color",
  "type": "object",
  "properties": {
    "hue": {
      "description": "hue attribute",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "saturation": {
      "description": "saturation attribute",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "level": {
      "description": "level attribute",
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "switch": {
      "description": "switch (onoff) attribute",
      "type": "string",
      "enum": ["on", "off"]
    },
    "hex": {
      "description": "color in hex, e.g. FFFFFF ",
      "type": "string",
      "pattern": "^[a-fA-F0-9]{6}$"
    }
  }
}
